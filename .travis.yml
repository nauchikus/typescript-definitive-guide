language: node_js
node_js:
- "11.1.0"
dist: trusty
sudo: false

branches:
  only:
  - app

cache:
  directories:
  - node_modules

install:
- npm i -g @vue/cli

script:
- vue --version
- npm run build
deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  local_dir: dist/
  on:
    branch: app
