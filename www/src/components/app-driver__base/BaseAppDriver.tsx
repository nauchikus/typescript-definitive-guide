import React, { FC, ReactNode } from "react";
import { AppDriver } from "../app-driver/AppDriver";

interface IAppDriverProps {
}

export const BaseAppDriver: FC<IAppDriverProps> = ( {  } ) => {
  return (
    <AppDriver>

    </AppDriver>
  );
};
