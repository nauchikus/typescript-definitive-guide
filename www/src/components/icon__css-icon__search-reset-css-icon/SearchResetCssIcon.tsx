import React, { FC } from "react";

interface ISearchResetCssIconProps {
}

export const SearchResetCssIcon: FC<ISearchResetCssIconProps> = ( {} ) => {
  return (
    <div className="search-reset-css-icon"></div>
  );
};